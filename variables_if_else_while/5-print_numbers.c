#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints
 * Return: Always (Success)
 */

int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	printf("%d", c);
	printf("\n");
	return (0);
}
